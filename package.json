{
  "name": "tbs-htph-clsk-monorepo",
  "version": "1.0.0",
  "description": "TBS Quality Control System - Monorepo",
  "private": true,
  "scripts": {
    "cf:build": "cd web-admin && npm ci && npm run cf:build",
    "cf:deploy": "cd web-admin && npx wrangler deploy",
    "cf:preview": "cd web-admin && npm run cf:preview"
  },
  "workspaces": [
    "web-admin",
    "mobile-app"
  ],
  "devDependencies": {
    "@types/node": "26.2.0",
    "@types/react": "19.2.18",
    "typescript": "7.0.2"
  }
}
